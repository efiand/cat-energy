<section class="hero">
  <div class="hero__container container">
    <div class="hero__content">
      <h1 class="hero__title">{{ title }}</h1>
      <p class="hero__text">{{ text }}</p>
    </div>

    {% include '../common/picture.twig' with picture %}

    <a class="hero__button button button--big-alter" href="#!">
      Подобрать программу
    </a>
  </div>
</section>
