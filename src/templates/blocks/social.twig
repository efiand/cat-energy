<ul class="social {{ className }}">
  {% for item in items %}
    <li class="social__item">
      <a class="social__link social__link--{{ item.id }}" href="{{ item.url }}">
        <span class="visually-hidden">{{ item.title }}</span>
      </a>
    </li>
  {% endfor %}
</ul>
